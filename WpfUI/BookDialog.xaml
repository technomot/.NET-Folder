<Window x:Class="WpfUI.BookDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Book Details"
        Height="420" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0 0 0 4"/>
            <Setter Property="Foreground" Value="#555555"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="6 5"/>
            <Setter Property="Margin" Value="0 0 0 14"/>
            <Setter Property="Height" Value="32"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="48"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Title" Style="{StaticResource Label}"/>
            <TextBox x:Name="TxtTitle"/>

            <TextBlock Text="Author" Style="{StaticResource Label}"/>
            <TextBox x:Name="TxtAuthor"/>

            <Grid Margin="0 0 0 14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Pages" Style="{StaticResource Label}"/>
                    <TextBox x:Name="TxtPages" Margin="0"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Price" Style="{StaticResource Label}"/>
                    <TextBox x:Name="TxtPrice" Margin="0"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Published date" Style="{StaticResource Label}"/>
            <DatePicker x:Name="DtpPublished"
                        FontSize="13"
                        Height="32"
                        Margin="0 0 0 14"/>

            <CheckBox x:Name="ChkAvailable"
                      Content="Available"
                      FontSize="13"
                      IsChecked="True"
                      Margin="0 4 0 0"/>
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center">
            <Button Content="Save"
                    Width="90" Height="34"
                    Margin="0 0 10 0"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BtnSave_Click"/>
            <Button Content="Cancel"
                    Width="90" Height="34"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>